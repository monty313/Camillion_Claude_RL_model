# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
/env/

# Data + model caches (NEVER commit large binaries)
data_cache/
# operator's private market data (MT5/broker exports) — huge + private; keep data/README.md tracked
data/*.csv
*.parquet
*.npy
*.npz
*.memmap
models/
checkpoints/
*.zip

# Notebooks
.ipynb_checkpoints/

# OS / editors
.DS_Store
.vscode/
.idea/

# Secrets (NEVER commit tokens/keys)
.env
*.token
secrets.*

# generated audit outputs (regenerate with: python tools/run_full_audit.py) — keep ASSUMPTIONS.md tracked
audit_results/audit_report.json
audit_results/audit_report.md
audit_results/audit_report.html
feature_cache/
